services:
  - type: web
    name: novasync-backend
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    autoDeploy: false
    envVars:
      - key: PORT
        value: "8000"
